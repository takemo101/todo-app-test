backend: pi
auto: false
create_pr: true
max_iterations: 100
preset: simple

worktree:
  enabled: true
  base_dir: ".worktrees"
  copy_files:
    - ".env"
    - ".envrc"
    - ".env.local"

session:
  manager: auto
  prefix: orch
  capture_interval: 500
